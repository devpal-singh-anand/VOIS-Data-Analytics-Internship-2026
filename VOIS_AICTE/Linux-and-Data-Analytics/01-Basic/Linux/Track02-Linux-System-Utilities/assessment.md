# VOIS Assessment — Linux System Utilities

## Questions and Answers

### 1. Which command displays performance information of the system?

* `$sudo apt-get install nmon`
* `$sudo install nmon apt-get`
* `$sudo install nmon apt`
* `$sudo install get nmon apt`

**Answer:** `$sudo apt-get install nmon`

---

### 2. Which command is used to find mounted file systems?

* `$nmon`
* `$findmnt`
* `$saider`
* `$findnt`

**Answer:** `$findmnt`

---

### 3. Which is used to display help and another argument for `saidar`?

* `saidar -h`
* `saidar -c`
* `saidar -v`
* `saidar -d`

**Answer:** `saidar -h`

---

### 4. Displays who is logged into the system and what process is executed by them.

* `w`
* `ls`
* `cd`
* `m`

**Answer:** `w`

---

### 5. Which displays ASCII-based graphic?

* `ncdu`
* `Slurm`
* `nmon`
* `saidar`

**Answer:** `nmon`